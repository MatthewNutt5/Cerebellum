### `/configs/`

This empty directory can be used to store local configs. Generally, any .json file not placed in the `/example/` directory will be ignored by Git, including any configurations stored here.
