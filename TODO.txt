Fixes/Refactors ==========

- Ordered shutdown of PSUList
    - In TestSettings, include a list of PSU indices that specifies the order
- Add more comments and update existing ones
- DeviceConfig interface?
    - PSUConfig, DAQConfig, etc.
    - Then have a PSUConfigList, a DAQConfigList, etc., and analogs to the
        PowerSupply interface for each
    - A DeviceConfig would generally specify how to access the device, and
        the behavior would be modulated by a PowerSupply or DAQ interface
    - In concert with the above task, could also refactor things into a
        DeviceConfig.py file
- Move some imports into __init__.py to help simplify?
- Consider switching from camelCase to snake_case for variables and functions

Key Features ==========

- Add new Event types
    - DAQ waveform retrieval
    - Interlock trigger (+ DAQ waveform triggered by falling edge)
- Add AnaGate support
- Add PSU dependencies (e.g. PSU X must be enabled before PSU Y)
- TBD: Add cold box control
    - Will again require calling custom scripts to keep things generalized
    - Could require a certain temperature reading before starting test
- Add storage of test results (e.g. csv file)

QoL Features ==========

- Add log verbosity
- Add log-to-file
- Adjust log formatting
- Persistence of GUI state
- Parsing in GUI to convert e.g. %addressRB% --> actual value of address
    in command events
- Some way to add custom interfaces to the GUI, i.e. as an option in the
    "interface" select, instead of requiring manual entry of interfaceName, etc.
    - implementationPath, implementationClass, implementationDisplayName
- preferences.json
    - Holds preferences like verbosity level
    - Would this be a good place to store peristence of GUI state? Or would that
        be called something more general like data.json?
- implementations.json
    - Front-end
        - On startup, GUI reads implementations specified by the JSON, and provides
            a dropdown menu of these implementations
        - Each implementation spec starts with the name, e.g. CAENPowerSupply
            - This name will be used as the import name and display name
            - If an implementation for power supply, handled in a PowerSupply folder
        - Each spec also includes the options available and their type
            - E.g. IP is a string, baudrate is an int, etc.
            - Would be cool but even more complicated to specify dropdown options
        - So the base case of each power supply is displayName and implementation,
            then the implementation decides the config options available
    - Back-end
        - Then PSUConfig would need to be refactored; each implementation should
            include its own config, e.g. CAENPowerSupply has CAENConfig
            - These configs extend some PSUConfig interface, and EnvironmentConfig
                stores a list of these generalized PSUConfigs
        - Then the PowerSupply constructor calls the right implementation
            constructor depending on its config type
            - The constructor is given by the implementations.json
- Build everything into a single .exe we can "release"?
    - Rename CerebellumGUI.py back to MainGUI.py and move it into the
        CerebellumGUI module, then use pyinstaller to compile everything
        starting from MainGUI.py
